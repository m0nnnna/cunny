# ── Secrets & environment ────────────────────────────────────────────
.env
env

# ── Server-specific nginx configs (contain real domains, IPs, SSL paths) ─
# Copy .example files and fill in your domain/paths; keep your copies ignored.
chatui.conf
synapse.conf
livekit.*.conf
token.*.conf
# Example templates (tracked): nginx-proxy.conf.example, livekit.nginx.conf.example, token.nginx.conf.example

# ── Build artifacts & archives ───────────────────────────────────────
old.build/
*.zip

# ── Node / npm ───────────────────────────────────────────────────────
node_modules/
cinny/node_modules/
livekit-token-server/node_modules/

# ── Build output ─────────────────────────────────────────────────────
cinny/dist/
*.tsbuildinfo

# ── Android build (generated on compile) ─────────────────────────────
cinny/android/build/
cinny/android/app/build/
cinny/android/.gradle/
cinny/android/local.properties
cinny/android/capacitor-cordova-android-plugins/
cinny/android/app/src/main/assets/public
cinny/android/app/src/main/assets/capacitor.config.json
cinny/android/app/src/main/assets/capacitor.plugins.json
cinny/android/app/src/main/res/xml/config.xml
*.apk
*.aab
*.ap_
NekoChat-release.apk

# ── Node / npm / yarn logs & cache ───────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn/
.pnp.*
.eslintcache
*.log

# ── OS / editor junk ────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.Zone.Identifier
?Zone.Identifier
desktop.ini

# ── IDE & editor ────────────────────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*.iml
.cursor/

# ── Plans / optional markdown (not needed to build) ───────────────────
docs/FORK-AND-IMPORT-PLAN.md
TROUBLESHOOTING.md

# ── Temp files from Claude/Cursor sessions ───────────────────────────
tmpclaude-*/
cinny/tmpclaude-*/
.gitignore
TROUBLESHOOTING.md
